# Supabase Functions Canonical Source

Canonical source of truth is `supabase/functions/*`.

Legacy mirror directories under `supabase_edge_functions/*` are deprecated and should not be edited.

Deprecated laptop-path functions now represented in canonical tree:
- `rapid-task`
- `signal-evaluator`
- `outlook-auth` (legacy compatibility route; canonical implementation is `outlook_auth`)
