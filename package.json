{
  "private": true,
  "scripts": {
    "frontend:lint": "yarn --cwd frontend lint",
    "frontend:lint:fix": "yarn --cwd frontend lint:fix",
    "supabase:validate:functions": "python scripts/validate_edge_functions.py",
    "supabase:validate:functions:strict": "python scripts/validate_edge_functions.py --strict-prereqs"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-keywords": "^5.1.0"
  }
}
